<header id="app-header-content">
    <h1><%= appName || 'Chatbot-playground' %></h1>
    <% if (typeof pageTitle !== 'undefined' && pageTitle) { %>
        <p>Page: <%= pageTitle %></p>
    <% } %>
</header>